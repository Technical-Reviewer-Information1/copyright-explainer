# 推奨コマンド

## アプリケーション実行
```bash
streamlit run streamlit_app.py
```

## 依存関係インストール
```bash
pip install -r requirements.txt
```

## 開発時のコマンド
- **ファイル構造確認**: `ls -la`
- **git状態確認**: `git status`
- **変更確認**: `git diff`

## 注意点
- このプロジェクトには特別なリント、フォーマット、テストツールは設定されていない
- 単一ファイル構成のため、アプリケーション実行のみでテスト可能